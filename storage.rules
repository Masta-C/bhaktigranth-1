rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Ebook PDFs — authenticated admin can upload; anyone can download (URL is gated by API)
    match /ebooks/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
